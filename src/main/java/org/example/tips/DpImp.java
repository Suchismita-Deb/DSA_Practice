package org.example.tips;

public class DpImp {

    public static void main(String[] args) {
        // When making the recursive solution then we see for the changing parameter.
        // In recursion say the parameter like index, count, prev value is chnging then we need the 3D array dp[index][count][prev].
        // Say the index value range from 0 to n,prev is one value and count can go till n. So total O(N^2)(n = index and n= count.)


        // When get something like minimum size of sub array think of sliding window as well.
    }
}